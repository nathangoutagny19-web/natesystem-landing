/**
 * `/hu/services/ia` : rend exactement le composant de `/services/ia`.
 * La langue vient de `app/hu/layout.tsx`, pas d'une copie du contenu.
 */
export { default } from '../../../services/ia/page'
