/**
 * `/hu/services/formation` : rend exactement le composant de `/services/formation`.
 * La langue vient de `app/hu/layout.tsx`, pas d'une copie du contenu.
 */
export { default } from '../../../services/formation/page'
