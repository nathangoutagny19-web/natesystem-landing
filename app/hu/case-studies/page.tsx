/**
 * `/hu/case-studies` : rend exactement le composant de `/case-studies`.
 * La langue vient de `app/hu/layout.tsx`, pas d'une copie du contenu.
 */
export { default } from '../../case-studies/page'
