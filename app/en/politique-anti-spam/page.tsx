/**
 * `/en/politique-anti-spam` : rend exactement le composant de `/politique-anti-spam`.
 * La langue vient de `app/en/layout.tsx`, pas d'une copie du contenu.
 */
export { default } from '../../politique-anti-spam/page'
/* ⚠︎ Metadonnees heritees du francais, a traduire. */
export { metadata } from '../../politique-anti-spam/page'
