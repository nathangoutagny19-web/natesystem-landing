/**
 * `/en/services/formation` : rend exactement le composant de `/services/formation`.
 * La langue vient de `app/en/layout.tsx`, pas d'une copie du contenu.
 */
export { default } from '../../../services/formation/page'
