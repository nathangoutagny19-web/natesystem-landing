/**
 * `/en/services/audit` : rend exactement le composant de `/services/audit`.
 * La langue vient de `app/en/layout.tsx`, pas d'une copie du contenu.
 */
export { default } from '../../../services/audit/page'
