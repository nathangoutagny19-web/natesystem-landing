/**
 * `/en/methode` : rend exactement le composant de `/methode`.
 * La langue vient de `app/en/layout.tsx`, pas d'une copie du contenu.
 */
export { default } from '../../methode/page'
/* ⚠︎ Metadonnees heritees du francais, a traduire. */
export { metadata } from '../../methode/page'
