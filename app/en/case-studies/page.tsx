/**
 * `/en/case-studies` : rend exactement le composant de `/case-studies`.
 * La langue vient de `app/en/layout.tsx`, pas d'une copie du contenu.
 */
export { default } from '../../case-studies/page'
