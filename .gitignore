# --- Regenerable / machine-specific (never needed in a clone) ---
node_modules
.next
out
tsconfig.tsbuildinfo
.claude
claude-seo
.playwright-mcp

# --- Secrets: must NEVER be committed ---
.env
.env.*
!.env.example
