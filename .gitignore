node_modules
.next
out
.claude
claude-seo

# Ignore PNG/JPG at the repo root (legacy mockups, exports) but keep
# everything that ships with the site under /public.
/*.png
/*.jpg
/nate-reviews-assets/*.png
